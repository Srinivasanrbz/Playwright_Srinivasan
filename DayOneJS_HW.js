// Declare a variable called `name` and assign it a string value. Declare variables `age` (number) and `isStudent` (boolean) as well. 

let Myname ="Srinivasan",age=28,IsStudent=false;
console.log('Name : '+Myname+'\n','Age : '+age+'\n','IsStudent : '+IsStudent,'\n');

// Use `const` to declare a variable for a constant value (e.g., the speed of light). Use `let` for another variable representing a 
// changeable value.

const price=1000;
const item="This charger is";
console.log(item+' '+price+' Rupees','\n');

// Create two string variables `firstName` and `lastName`. Concatenate them and print the full name.

let firstName="Srinivasan";
let lastName="Panneerselvam";
console.log(firstName+' '+lastName,'\n');

//Addition:- Declare two variables `a` and `b` with numeric values. Print their sum to the console.
let a=50,b=50;
console.log('Sum Total : '+ (a+b),'\n')

//Multiplication:- Declare a variable `x` with the value of 10 and `y` with the value of 15. Print the result of their multiplication.
let  x=25,y=4;
console.log('Multiplication Total : '+ (x*y),'\n')

// Perimeter of a Rectangle:- Declare two variables `length` and `width`. Assign values to them and calculate the perimeter of the rectangle. 
// Print the result.
let length=10,width=5;
result=2*(length+width)
console.log('Perimeter of rectangle is : '+result)